Task contract (ticket: fix task queue normalize and display bugs):
- Objective: the task-runner CLI has @FAILS@ test failures across normalize, format, status, and create commands. Diagnose and fix the root causes so ALL @TOTAL@ tests pass.
- allowed_files: ONLY @ALLOWED@ in the current directory.
- must_preserve: all function names and signatures; version numbers; all existing utility functions in queue-lib.sh; task record pipe-delimited format (id|name|priority|status|created).
- forbidden_changes: run-tests.sh (test file), any new files. Do NOT weaken, skip, or mask failing checks.
- acceptance_test: sh ./run-tests.sh prints pass=@TOTAL@ fail=0.
- Max 2 fix iterations. Work only in the current directory. When done, state PASS or FAIL based on the final test run.
