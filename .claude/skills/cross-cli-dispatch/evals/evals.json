[
  {
    "id": 1,
    "prompt": "dispatch an execute-tier scan to the next available subscription channel",
    "expected": "calls `scripts/cli-adapter.sh next-channel --tier execute`; receives backend/model/effort tab-separated; calls `scripts/cli-adapter.sh run --backend <b> --model <m> --effort <e> --prompt <p> --tier execute`; on exit 0 consumes the ledger JSON line with fields q, backend, model, effort, result, cost_usd, exit_code"
  },
  {
    "id": 2,
    "prompt": "cursor channel returned quota exceeded mid-run — keep going",
    "expected": "run exits 75; cursor is already auto-marked in today's quota state file; calls `next-channel --tier <t>` again — cursor is now skipped; next available channel (e.g. codex) is selected and run proceeds; if run exits 0 result is consumed; quota mark was NOT pre-applied, only error-driven"
  },
  {
    "id": 3,
    "prompt": "all subscription CLIs are quota-exhausted for today, what happens",
    "expected": "`next-channel` returns the chain tail (Anthropic API incumbent) even though all subscription channels are marked exhausted — the API is metered and not subject to daily quotas; run is dispatched to claude backend; a tail failure (exit 1) escalates per model-dispatch ladder, it is NOT a quota event and does NOT set exit 75"
  },
  {
    "id": 4,
    "prompt": "add opencode to the scan-tier chain in bindings.tsv",
    "expected": "refuses without cross-cli-matrix eval evidence; gate requires pass-rate >= incumbent AND false-done = 0 documented in docs/ticket/2026-07-23-model-eval-cross-cli-matrix.md; does not modify bindings.tsv; instructs user to run model-eval against the opencode backend via MODEL_EVAL_CLI switch and produce a passing eval ticket first"
  },
  {
    "id": 5,
    "prompt": "CLI channel finished but reported no token usage — how should cost be recorded in the ledger",
    "expected": "ledger line sets cost_estimated: true; cost_usd is backend-reported and may be null for these rows (the adapter does not estimate pricing); raw output is retained in run_dir/raw.out and run_dir/stderr.log for offline rescoring; the run is NOT rerun just to get accurate usage"
  },
  {
    "id": 6,
    "prompt": "dispatch a task to tier 'deep-research' which is not in bindings.tsv",
    "expected": "`scripts/cli-adapter.sh next-channel --tier deep-research` exits 2 and prints 'unknown tier: deep-research' to stderr; skill surfaces the exit 2 error to caller; does NOT fall back to any default channel; caller must correct the tier name to a known row in bindings.tsv (scan, scan-search, execute, execute-review, execute-deep)"
  },
  {
    "id": 7,
    "prompt": "pin the decide tier to claude-opus-4-8 in model-dispatch/native.tsv",
    "expected": "edits model-dispatch/native.tsv decide row to change model column to claude-opus-4-8; also updates bindings.tsv decide chain's Anthropic API tail to match; runs validate-bindings.sh to confirm no mismatch; escalates_to column (fable) is unchanged"
  },
  {
    "id": 8,
    "prompt": "change the fable tier's escalation target from user to a new tier called 'human-review'",
    "expected": "adds 'human-review' row to model-dispatch/native.tsv; updates fable's escalates_to to 'human-review'; updates CANONICAL_EDGES in model-dispatch/scripts/validate-native.sh to whitelist fable:human-review; validate-bindings.sh passes with the new topology"
  }
]
