---
name: py-edge-skill
description: Synthetic fixture for the deadcode-audit python-edge fix. Use when validating python import/call edges, the test-only import boundary, and example-literal dangling suppression. Triggers on reachability python-edge fixture checks.
---

# py-edge-skill

Run the entry point:

```bash
python3 scripts/audit.py
```

See [references/sample.md](references/sample.md) for a sample report.
