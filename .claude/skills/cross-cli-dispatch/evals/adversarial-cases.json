{
  "description": "Adversarial inputs for cross-cli-dispatch: boundary cases that should NOT silently pass.",
  "cases": [
    {
      "id": "adv-cycle-escalation",
      "input_kind": "native-tsv-edit",
      "description": "Create a cycle in escalates_to edges (fable→decide when decide→fable exists)",
      "expected": "validate-bindings.sh fails with 'cycle detected' message; edit rejected"
    },
    {
      "id": "adv-unknown-effort",
      "input_kind": "cli-adapter-run",
      "description": "Pass --effort xhigh to cli-adapter.sh run",
      "expected": "exit 1 with 'invalid effort' message; run does NOT proceed with the unknown label"
    },
    {
      "id": "adv-off-ladder-edge",
      "input_kind": "native-tsv-edit",
      "description": "Set scan escalates_to decide (not in CANONICAL_EDGES)",
      "expected": "validate-bindings.sh fails with 'off-ladder escalation' message"
    },
    {
      "id": "adv-bindings-native-mismatch",
      "input_kind": "tsv-edit",
      "description": "Edit model-dispatch/native.tsv model without updating bindings.tsv API tail",
      "expected": "validate-bindings.sh fails with 'model-dispatch/native.tsv mismatch' message"
    }
  ]
}
