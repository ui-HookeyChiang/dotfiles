---
name: example-skill
description: An example skill demonstrating an explicit deprecation keyword case.
---

# Example Skill

## Configuration

The `--legacy-mode` flag is **deprecated** and will be removed in the next
release cycle. New callers should pass `--strict` instead.

This is a regular prose paragraph that does not contain a marker.
