---
title: "Skip-mode fixture (300 words)"
kind: spec
slug: spec-advisory-skip-fixture
date: 2026-05-21
status: proposed
---

# Skip-mode fixture

## Purpose

Fixture used by spec-advisory tests. Word count below 500 so the router
emits mode=skip.

## Goals

Trigger the skip branch in spec-advisory.sh full mode.

## Design

Filler body follows; word count is engineered to be ~300.

count criterion text stdout word finding high low fixture flow light validates finding advisory
flow severity constraint spec high exits deep architecture validates two advisory advisory advisory fixture
snippet spec flow light severity advisory diff deep high finding section deep lane deep
deep medium zero advisory gate section fixture validates decision plan zero word manual rewrite
severity rewrite skip one zero architecture finding rewrite oracle architecture fixture low emits oracle
gate decision autonomous section autonomous body high rewrite validates router diff oracle autonomous finding
advisory low fixture one test architecture architecture oracle fixture router router rewrite deep spec
skip snippet section deep oracle rewrite lane criterion lane medium exits section constraint spec
flow rewrite count diff section light severity filler low autonomous criterion section skip rewrite
gate finding lane gate lane spec snippet snippet test test manual medium constraint advisory
deep plan decision section architecture decision body section stdout fixture text body advisory high
spec exits emits exits word test decision lane zero text router router stdout diff
router exits fixture zero medium two finding low word advisory one flow manual gate
skip stdout validates stdout rewrite light constraint severity advisory deep advisory oracle threshold fixture
router high rewrite severity snippet deep plan diff high deep diff fixture advisory oracle
criterion two plan severity filler one count light filler one text text one one
router gate criterion stdout count spec section fixture architecture light criterion medium router test
rewrite fixture flow skip lane validates light criterion severity architecture skip finding validates flow
zero rewrite finding advisory two test oracle zero
