---
name: example-skill
description: An example skill demonstrating an HTML-comment legacy marker.
---

# Example Skill

## Migration notes

<!-- legacy: the old --json-output flag was renamed to --format=json in
v0.4. Remove this comment once all callers verified post-rebase. -->

The current flag is `--format=json`.
