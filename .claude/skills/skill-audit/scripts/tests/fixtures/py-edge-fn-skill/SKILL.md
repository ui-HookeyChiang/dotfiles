---
name: py-edge-fn-skill
description: Synthetic fixture for the deadcode-audit function-edge fix. Use when validating module-alias-qualified call edges, self method and property access edges, the dunder advisory demotion, and the unresolved-qualifier collision guard. Triggers on reachability function-edge fixture checks.
---

# py-edge-fn-skill

Run the entry point:

```bash
python3 scripts/audit.py
```
