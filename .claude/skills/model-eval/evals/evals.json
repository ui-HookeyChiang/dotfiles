[
  {
    "id": 1,
    "prompt": "Benchmark haiku vs sonnet-5 on the executor tier, n=1, v1 fixture",
    "expected": "runs scripts/run-executor.sh once per model with pinned --model and effort low; each run appends one JSON line to $MODEL_EVAL_OUT/ledger.jsonl with pass, test_last matching the v1 suite total (pass=10 when green), cost_usd, out_tok; aggregate.py prints pass and cost/pass per model"
  },
  {
    "id": 2,
    "prompt": "Run one scanner eval with claude-haiku-4-5-20251001",
    "expected": "scripts/run-scanner.sh sends the corpus with the classification prompt, parses the JSON answer, scores 24 fields against fixtures/scanner/truth.json, appends a ledger line with accuracy and parse_ok"
  },
  {
    "id": 3,
    "prompt": "Compare tokenizer inflation between sonnet-4-6 and sonnet-5 for zh and en",
    "expected": "scripts/run-tokenizer.sh runs a verbatim-repeat per (model, lang); ledger records out_tok and chars_out; comparison uses out_tok ratio on identical text, excluding runs whose chars_out deviates from the source (compliance blip per references/harness-pitfalls.md)"
  },
  {
    "id": 4,
    "prompt": "Eval opus-4-6 as planner in no-context mode against this repo",
    "expected": "scripts/run-planner.sh with mode=no-context withholds the context file, gives only the objective head, runs inside repo_dir with --max-turns 20, writes plan.yaml; scoring uses references/planner-checklist.md plus discovery-cost and fabrication/abstention notes per references/planner-modes.md"
  },
  {
    "id": 5,
    "prompt": "The whole executor batch shows every model passing — what is the verdict?",
    "expected": "declares ceiling: pass rate has no discrimination, verdict moves to the cost axis (cost/pass), or raise difficulty via fixture knobs in references/fixture-design.md; does NOT claim one model is more capable from a ceiling result"
  }
]