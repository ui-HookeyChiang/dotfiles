{
  "trap_field": "is_btrfs_defective",
  "trap_question": "the crash surfaces through btrfs frames \u2014 is btrfs itself defective?",
  "root_causes": [
    {
      "id": "unlocked-plug-add",
      "desc": "release_stripe_plug adds sh->lru to cb->list without conf->device_lock",
      "accept_any": [
        "release_stripe_plug",
        "plug"
      ]
    },
    {
      "id": "missing-device-lock",
      "desc": "the add is the only sh->lru writer holding no lock; all other writers hold conf->device_lock \u2014 concurrent list_del_init corrupts",
      "accept_any": [
        "device_lock",
        "without a lock",
        "no lock",
        "unserialized",
        "unserialised",
        "not locked",
        "lockless"
      ]
    }
  ],
  "note": "Fix = PR debbox-kernel#1616: take device_lock around the plug-list list_add_tail."
}