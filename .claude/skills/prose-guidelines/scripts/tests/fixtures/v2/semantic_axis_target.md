# Semantic axis fixture target

This is a long sample paragraph used by the semantic_axis validator fixtures. It contains enough words to look like a real prose paragraph in a SKILL.md or engineering spec body. The paragraph spans several sentences so it qualifies under the prose-guidelines paragraph extraction rules.

We leverage the cache to facilitate faster reads across the entire production cluster.

Some closing prose so the file has enough lines for line-range checks.
