#!/usr/bin/env bash
# dispatch-e2e-baseline-agent.sh — RETIRED (ADR 0008)
#
# Replaced by dispatch-expectation-acceptance-agent.sh
# The AB comparison protocol is retired; use INTENT-frozen expectation specs instead.

echo "dispatch-e2e-baseline-agent: this script is retired (ADR 0008)." >&2
echo "  Use: bash skill-writer/scripts/dispatch-expectation-acceptance-agent.sh <skill-path>" >&2
exit 1
